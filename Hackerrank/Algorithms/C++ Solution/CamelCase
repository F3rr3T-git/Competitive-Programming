#include <bits/stdc++.h>
using namespace std;

int main ()
{
    int i,c=0;
    string s;
    cin >> s;
    

    for(i=0;i<s.length();i++)
    {
         if(int(s[i])>=65 && int(s[i])<=90)
         c++;
    }

    cout << c+1 << endl;

return 0;
}
