#include <bits/stdc++.h>
using namespace std;

int main ()
{
    int i,n,r,a[101]={0},c=0;
    cin >> n;

    for(i=0;i<n;i++)
    {
       cin >> r;
       a[r]++;
    }

    for(i=0;i<=100;i++)
    {
        c += a[i]/2;
    }

    cout << c << endl;

return 0;
}
