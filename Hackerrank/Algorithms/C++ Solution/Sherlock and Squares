#include<bits/stdc++.h>
using namespace std;

int main()
{
  
int t;
cin >> t;

while(t--)
{

 double a,b;
 cin >> a >> b;

  cout << (int)( floor(sqrt(b))-ceil(sqrt(a))+1 ) << endl;

}

  return 0;
}
