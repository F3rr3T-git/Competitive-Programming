#include <bits/stdc++.h>
using namespace std;

int main ()
{
    int i,n,k,r,mx=0;
    cin >> n >> k;

    for(i=0;i<n;i++)
    {
       cin >> r;
       mx = max(mx,r);
    }

    if(k>=mx)
    cout << 0 << endl;
    else 
    cout << mx-k << endl;

return 0;
}

