#include<bits/stdc++.h>
using namespace std;

int main()
{
  int i,ar[51],r,n;
  cin >> n;

  for(i=1;i<=n;i++)
  {
    cin >> r;
    ar[r] = i;
  }

  for(i=1;i<=n;i++)
  cout << ar[ar[i]] << endl;

  return 0;
}
