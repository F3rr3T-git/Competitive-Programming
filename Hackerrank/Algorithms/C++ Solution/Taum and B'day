#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    long long int b,w,bc,wc,z;
    cin >> t;
    
    while(t--)
    {

    cin >> b >> w;
    cin >> bc >> wc >> z;

    cout << b*min(bc,(wc+z)) + w*min(wc,(bc+z)) << endl;

    }
    
    return 0;
}
