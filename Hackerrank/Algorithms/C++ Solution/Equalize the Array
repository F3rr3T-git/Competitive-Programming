#include <bits/stdc++.h>
using namespace std;

int main()
{

   int n,i,r,m=0;
   cin >> n;
   int ar[101]={0};

   for(i=0;i<n;i++)
   {
      cin >> r;
      ar[r]++;
   }
   
   for(i=1;i<=101;i++)
   {
     m = max(m,ar[i]);
   }


    cout << n - m << endl;
    return 0;
}
