#include <bits/stdc++.h>
 
using namespace std;
 
int main ()
{
      int i,n,sm=0,r;
      cin >> n;
      
      for(i=0;i<n;i++)
      {
        cin >> r;
        sm += r;
      }
      
      cout << fixed << setprecision (12) << (double)sm/(n) << endl;
      
      return 0;
      
}
